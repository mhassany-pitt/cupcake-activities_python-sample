$schema: https://learning-contents.org/schemas/worked-example/0.1.0
source: main.py
license: MIT
locale: en-US
id: we_loop_range
title: Summing a Range
description: Loops and range demo.
authors:
  - first: Ethan
    last: Hunt
    email: ethan@impossible.com
    affiliation: Independent researchers
pedagogy:
  activity: worked-example
  difficulty: novice
  topics:
    - loops
    - range
  prereq_topics: []
runtime:
  language: python
  version: 3
statement: |
  Learn how to compute the sum of numbers in a range using a `for` loop. This worked example covers:
  - Accumulating values in a loop variable
  - Generating sequences using the `range(start, stop)` generator
executions:
  - title: Sum 1..5
    stdout: "Total sum is: 15"
    console: "Total sum is: 15"
    status: pass
elements:
  - type: explanation
    location:
      snippet: range(1, 6)
    explanations:
      - body: |
          The `range(start, stop)` function generates integers from `start` up to (but not including) `stop`. Therefore, `range(1, 6)` generates the values `1, 2, 3, 4, 5`.
